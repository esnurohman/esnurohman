import React from "react";

const Pattern = () => {
  return (
    // pattern from https://heropatterns.com/
    <div className="hidden md:flex absolute left-24 top-0 w-full h-full bg-[url(/assets/pattern.svg)] bg-center bg-no-repeat bg-cover xl:bg-contain xl:-top-[110px] -z-10 text-accent "></div>
  );
};

export default Pattern;
